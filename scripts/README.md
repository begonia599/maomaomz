# 🚀 自动发布工具

## 快速使用

### 方法1：一键发布（推荐）

```bash
npm run release
```

**这个命令会自动完成：**
1. ✅ 构建前端代码
2. ✅ 提交所有更改到 Git
3. ✅ 推送代码到 GitHub
4. ✅ 创建并推送 Git tag
5. ✅ 创建 GitHub Release（如果安装了 `gh` CLI）

---

## 安装 GitHub CLI（可选，但推荐）

安装后可以自动创建 GitHub Release，无需手动操作。

### Windows (PowerShell):
```powershell
winget install --id GitHub.cli
```

或下载：https://cli.github.com/

### 首次使用需要登录：
```bash
gh auth login
```

---

## 发布流程

### 1. 更新版本号

修改以下文件中的版本号：
- `package.json` → `"version": "1.5.2"`
- `manifest.json` → `"version": "1.5.2"`
- `manifest.json` → 添加 `"1.5.2": "更新日志内容"`

### 2. 运行发布命令

```bash
npm run release
```

### 3. 完成！

- 如果安装了 `gh` CLI：自动创建 GitHub Release ✅
- 如果没有安装：脚本会提示你手动创建 Release 的链接

---

## 手动发布（不推荐）

如果 `npm run release` 失败，可以手动执行：

```bash
# 1. 构建
npm run build

# 2. 提交并推送
git add -A
git commit -m "chore: release v1.5.2"
git push

# 3. 创建 tag
git tag -a v1.5.2 -m "Release v1.5.2"
git push origin v1.5.2

# 4. 手动创建 GitHub Release
# 访问: https://github.com/mzrodyu/maomaomz/releases/new
```

---

## 常见问题

### Q: 提示 "gh: command not found"
**A:** 没有安装 GitHub CLI，脚本会提示手动创建 Release 的链接，按提示操作即可。

### Q: 推送失败 "rejected"
**A:** 先运行 `git pull --rebase`，然后重新运行 `npm run release`

### Q: 如何撤销发布？
**A:** 
```bash
# 删除本地 tag
git tag -d v1.5.2

# 删除远程 tag
git push origin --delete v1.5.2

# 在 GitHub 手动删除 Release
```

---

## 提示

- ✅ 发布前确保本地代码已测试通过
- ✅ 更新日志写得详细一点，用户体验更好
- ✅ 版本号遵循语义化版本（Semantic Versioning）
  - `1.5.1` → `1.5.2` (小改动、bug修复)
  - `1.5.1` → `1.6.0` (新功能)
  - `1.5.1` → `2.0.0` (重大更新、不兼容变更)
